- project:
    name: mks-argocd-prod
    jobs:
      - '{name}-yaml':
          organisation: 'MKS'
      - '{name}-argocd-diff':
          organisation: 'MKS'
      - '{name}-pre-commit-all':
          organisation: 'MKS'
      - '{name}-mks-release-notes':
          organisation: 'MKS'
- project:
    name: mks-argocd-test
    jobs:
      - '{name}-yaml':
          organisation: 'MKS'
      - '{name}-argocd-diff':
          organisation: 'MKS'
      - '{name}-pre-commit-all':
          organisation: 'MKS'
      - '{name}-mks-release-notes':
          organisation: 'MKS'
- project:
    name: mks-argocd-clusters-prod
    jobs:
      - '{name}-yaml':
          organisation: 'MKS'
      - '{name}-pre-commit-all':
          organisation: 'MKS'
- project:
    name: mks-argocd-clusters-test
    jobs:
      - '{name}-yaml':
          organisation: 'MKS'
      - '{name}-pre-commit-all':
          organisation: 'MKS'
