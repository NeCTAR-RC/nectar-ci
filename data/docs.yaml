- project:
    name: nectarcloud-tier0doco
    jobs:
      - '{name}-markdown':
          organisation: 'NeCTAR-RC'
- project:
    name: nectar-specs
    jobs:
      - '{name}-dummy':
          organisation: 'NeCTAR-RC'
- project:
    name: tutorials
    jobs:
      - '{name}-pre-commit-all':
          organisation: 'NeCTAR-RC'
      - '{name}-jekyll-test-build':
          organisation: 'NeCTAR-RC'
      - '{name}-jekyll-html-check':
          organisation: 'NeCTAR-RC'
      - '{name}-jekyll-test-build-cleanup':
          organisation: 'NeCTAR-RC'
- project:
    name: nectar-architecture
    jobs:
      - '{name}-dummy':
          organisation: 'internal'
