- project:
    name: nectarcloud-tier0doco
    jobs:
      - '{name}-markdown':
         access: 'anonymous'
         organisation: 'NeCTAR-RC'

- project:
    name: nectar-specs
    jobs:
      - '{name}-dummy':
         access: 'anonymous'
         organisation: 'NeCTAR-RC'

- project:
    name: nectar-training
    jobs:
      - '{name}-jekyll-build':
         access: 'anonymous'
         organisation: 'NeCTAR-RC'
      - '{name}-jekyll-deploy':
         access: 'anonymous'
         organisation: 'NeCTAR-RC'

- project:
    name: tutorials
    jobs:
      - '{name}-jekyll-test-build':
         access: 'anonymous'
         organisation: 'NeCTAR-RC'
