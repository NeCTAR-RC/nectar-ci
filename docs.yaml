- project:
    name: nectarcloud-tier0doco
    jobs:
      - '{name}-markdown':
         access: 'anonymous'
         organisation: 'NeCTAR-RC'

- project:
    name: nectar-specs
    jobs:
      - '{name}-dummy':
         access: 'anonymous'
         organisation: 'NeCTAR-RC'

- project:
    name: rally-jobs
    jobs:
      - '{name}-dummy':
         access: 'anonymous'
         organisation: 'NeCTAR-RC'
