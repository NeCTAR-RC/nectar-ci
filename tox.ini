[tox]
envlist = tools
skipsdist = True

[testenv:tools]
description = Unit tests for tools/audit_projects.py
deps =
    pytest
    ruamel.yaml>=0.17
    requests>=2.25
commands =
    pytest {posargs:tools/tests}
