- project:
    name: freshdesk-pip
    jobs:
      - '{name}-nodejs-test':
         access: 'anonymous'
         organisation: 'NeCTAR-RC'

- project:
    name: freshdesk-openstack
    jobs:
      - '{name}-nodejs-test':
         access: 'anonymous'
         organisation: 'NeCTAR-RC'
