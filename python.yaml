- project:
    name: hivemind
    jobs:
      - '{name}-python-tox-{environment}':
         organisation: 'NeCTAR-RC'
         environment: 'py27'
      - '{name}-python-tox-{environment}':
         organisation: 'NeCTAR-RC'
         environment: 'flake8'

- project:
    name: hivemind_contrib
    jobs:
      - '{name}-python-tox-{environment}':
         organisation: 'NeCTAR-RC'
         environment: 'py27'
      - '{name}-python-tox-{environment}':
         organisation: 'NeCTAR-RC'
         environment: 'flake8'

- project:
    name: hive_master
    jobs:
      - '{name}-run-tests':
         organisation: 'NeCTAR-RC'

- project:
    name: langstroth
    jobs:
      - '{name}-run-tests':
         organisation: 'NeCTAR-RC'

- project:
    name: nectar-tools
    jobs:
      - '{name}-run-tests':
         organisation: 'NeCTAR-RC'

