- job:
    name: tempest-compute-host-check
    defaults: global
    node: tempest
    concurrent: true
    scm:
      - git:
         url: ssh://review.rc.nectar.org.au:29418/internal/nectar-testing.git
         credentials-id: 4946c3a5-9f5e-4eac-9ec4-90e1e348db14
    builders:
      - shell: |
          #!/bin/bash
          echo
          echo "================================================================="
          echo
          echo "                   CLOUD: $CLOUD"
          echo "                   AZ:    $AVAILABILITY_ZONE"
          echo "                   HOST:  $HOST"
          echo
          echo "================================================================="
          echo
          . /opt/tempest/bin/activate
          tmpdir=`mktemp -d --suffix=_tempest`
          cd $WORKSPACE/tempest
          ./setup_tempest.py --host $HOST -s $AVAILABILITY_ZONE -e $CLOUD -j check-compute-host $tmpdir
          cd $tmpdir
          ostestr --serial -w $WORKSPACE/tempest/whitelists/check-compute-host.yaml 2>&1 | grep --line-buffered -vE ' \w+Warning: |self._sock = None'
          RET=${PIPESTATUS[0]}
          rm -rf $tmpdir
          exit $RET
    parameters:
      - choice:
          name: 'CLOUD'
          choices:
            - production
            - testing
            - development
          description: "Cloud to run tempest on (Production, Test, Dev). Leave as Production if unsure."
      - string:
          name: 'AVAILABILITY_ZONE'
          description: 'Nova availability zone. <a href="https://wiki.rc.nectar.org.au/wiki/Tempest#AVAILABILITY_ZONES">List of Availability Zones</a>'
      - string:
          name: 'HOST'
          description: |
                Nova host to test. <br>
                - Host must be in AVAILABILITY_ZONE; if not nova will return a 'No Valid Host' error.<br>
                - Leave blank to let scheduler choose a host.
    properties:
      - authorization:
          'nobody':
            - job-read
            - job-discover

- job:
    name: tempest-cinder-check
    defaults: global
    node: tempest
    concurrent: true
    scm:
      - git:
         url: ssh://review.rc.nectar.org.au:29418/internal/nectar-testing.git
         credentials-id: 4946c3a5-9f5e-4eac-9ec4-90e1e348db14
    builders:
      - shell: |
          #!/bin/bash
          . /opt/tempest/bin/activate
          tmpdir=`mktemp -d --suffix=_tempest`
          cd $WORKSPACE/tempest/
          ./setup_tempest.py -s $AVAILABILITY_ZONE -e $CLOUD -j check_cinder $tmpdir
          cd $tmpdir
          ostestr --serial -w $WORKSPACE/tempest/whitelists/check-cinder.yaml 2>&1 | grep --line-buffered -vE ' \w+Warning: |self._sock = None'
          RET=${PIPESTATUS[0]}
          rm -rf $tmpdir
          exit $RET
    parameters:
      - choice:
          name: 'CLOUD'
          choices:
            - production
            - testing
            - development
          description: "Cloud to run tempest on (Production, Test, Dev). Leave as Production if unsure."
      - string:
          name: 'AVAILABILITY_ZONE'
          description: 'Cinder availability zone. <a href="https://wiki.rc.nectar.org.au/wiki/Tempest#AVAILABILITY_ZONES">List of Availability Zones</a>'
    properties:
      - authorization:
          'nobody':
            - job-read
            - job-discover

- job:
    name: tempest-neutron-check
    defaults: global
    node: tempest
    concurrent: true
    scm:
      - git:
         url: ssh://review.rc.nectar.org.au:29418/internal/nectar-testing.git
         credentials-id: 4946c3a5-9f5e-4eac-9ec4-90e1e348db14
    builders:
      - shell: |
          #!/bin/bash
          . /opt/tempest/bin/activate
          tmpdir=`mktemp -d --suffix=_tempest`
          cd $WORKSPACE/tempest/
          ./setup_tempest.py -e $CLOUD -j nagios-network $tmpdir
          cd $tmpdir
          ostestr --serial -w $WORKSPACE/tempest/whitelists/check-neutron.yaml 2>&1 | grep --line-buffered -vE ' \w+Warning: |self._sock = None'
          RET=${PIPESTATUS[0]}
          rm -rf $tmpdir
          exit $RET
    parameters:
      - choice:
          name: 'CLOUD'
          choices:
            - production
            - testing
            - development
          description: "Cloud to run tempest on (Production, Test, Dev). Leave as Production if unsure."
    properties:
      - authorization:
          'nobody':
            - job-read
            - job-discover

- job:
    name: tempest-scenario-check
    defaults: global
    node: tempest
    concurrent: true
    scm:
      - git:
         url: ssh://review.rc.nectar.org.au:29418/internal/nectar-testing.git
         credentials-id: 4946c3a5-9f5e-4eac-9ec4-90e1e348db14
    builders:
      - shell: |
          #!/bin/bash
          . /opt/tempest/bin/activate
          tmpdir=`mktemp -d --suffix=_tempest`
          cd $WORKSPACE/tempest/
          ./setup_tempest.py --host $HOST -s $AVAILABILITY_ZONE -e $CLOUD -j nagios-network $tmpdir
          cd $tmpdir
          ostestr --serial -w $WORKSPACE/tempest/whitelists/check-scenario.yaml 2>&1 | grep --line-buffered -vE ' \w+Warning: |self._sock = None'
          RET=${PIPESTATUS[0]}
          rm -rf $tmpdir
          exit $RET
    parameters:
      - choice:
          name: 'CLOUD'
          choices:
            - production
            - testing
            - development
          description: "Cloud to run tempest on (Production, Test, Dev). Leave as Production if unsure."
      - string:
          name: 'AVAILABILITY_ZONE'
          description: 'Nova availability zone. <a href="https://wiki.rc.nectar.org.au/wiki/Tempest#AVAILABILITY_ZONES">List of Availability Zones</a>'
      - string:
          name: 'HOST'
          description: |
                Nova host to test. <br>
                - Host must be in AVAILABILITY_ZONE; if not nova will return a 'No Valid Host' error.<br>
                - Leave blank to let scheduler choose a host.
    properties:
      - authorization:
          'nobody':
            - job-read
            - job-discover

- job:
    name: tempest-community-image-check
    defaults: global
    node: tempest
    concurrent: true
    scm:
      - git:
         url: ssh://review.rc.nectar.org.au:29418/internal/nectar-testing.git
         credentials-id: 4946c3a5-9f5e-4eac-9ec4-90e1e348db14
    builders:
      - shell: |
          #!/bin/bash
          . /opt/tempest/bin/activate
          tmpdir=`mktemp -d --suffix=_tempest`
          cd $WORKSPACE/tempest/
          ./setup_tempest.py --image $IMAGE_ID -e production -j community-images $tmpdir
          cd $tmpdir
          ostestr --serial -w $WORKSPACE/tempest/whitelists/community-images.yaml 2>&1 | grep --line-buffered -vE ' \w+Warning: |self._sock = None'
          RET=${PIPESTATUS[0]}
          rm -rf $tmpdir
          exit $RET
    parameters:
      - string:
          name: 'IMAGE_ID'
          description: |
                Id of the image to test. <br>
    properties:
      - authorization:
          'nobody':
            - job-read
            - job-discover

- project:
    name: tempest-nagios-site-compute
    site:
        - auckland
        - intersect-01
        - intersect-02
        - melbourne-np
        - melbourne-qh2
        - monash-01
        - monash-02
        - nci
        - qld
        - sa
        - swinburne-01
        - tasmania
    jobs:
      - 'tempest-nagios-site-{site}-{test}':
          test:
            - 'compute'
      - 'tempest-cleanup-site-{site}'

- project:
    name: tempest-nagios-site-network
    site:
        - melbourne-np
        - melbourne-qh2
        - qld
        - tasmania
    jobs:
      - 'tempest-nagios-site-{site}-{test}':
          test:
            - 'network'

- project:
    name: tempest-nagios-flavor
    flavor:
        - tiny
        - xsmall
        - small
        - medium
        - large
        - xlarge
    jobs:
      - 'tempest-nagios-flavor-{flavor}-{test}':
          test:
            - 'compute'


- job-template:
    name: 'tempest-nagios-site-{site}-{test}'
    defaults: global
    node: tempest
    concurrent: false

    scm:
      - git:
         url: ssh://review.rc.nectar.org.au:29418/internal/nectar-testing.git
         credentials-id: 4946c3a5-9f5e-4eac-9ec4-90e1e348db14

    builders:
      - shell: |
          . /opt/tempest/bin/activate
          timeout 20m $WORKSPACE/nagios/tempest_nagios.py -c $WORKSPACE/nagios/tempest_nagios.conf -s {site} -t {test} -e production

    triggers:
      - timed: 'H/30 * * * *'

    publishers:
      - naginator:
          max-failed-builds: 4
          fixed-delay: 120
      - trigger:
          project: 'tempest-cleanup-site-{site}'
          threshold: 'FAILURE'

    properties:
      - authorization:
          'anonymous':
            - job-read
            - job-discover
      - build-discarder:
          days-to-keep: 7


- job-template:
    name: 'tempest-nagios-flavor-{flavor}-{test}'
    defaults: global
    node: tempest
    concurrent: false

    scm:
      - git:
         url: ssh://review.rc.nectar.org.au:29418/internal/nectar-testing.git
         credentials-id: 4946c3a5-9f5e-4eac-9ec4-90e1e348db14

    builders:
      - shell: |
          . /opt/tempest/bin/activate
          timeout 45m $WORKSPACE/nagios/tempest_nagios.py -c $WORKSPACE/nagios/tempest_nagios.conf -f {flavor} -t {test} -e production

    triggers:
      - timed: 'H * * * *'

    publishers:
      - naginator:
          max-failed-builds: 4
          fixed-delay: 120
      - trigger:
          project: 'tempest-cleanup'
          threshold: 'FAILURE'

    properties:
      - authorization:
          'anonymous':
            - job-read
            - job-discover
      - build-discarder:
          days-to-keep: 7

- job-template:
    name: 'tempest-cleanup-site-{site}'
    defaults: global
    node: tempest
    concurrent: false

    scm:
      - git:
         url: ssh://review.rc.nectar.org.au:29418/internal/nectar-testing.git
         credentials-id: 4946c3a5-9f5e-4eac-9ec4-90e1e348db14

    builders:
      - shell: |
          . /opt/tempest/bin/activate
          timeout 5m $WORKSPACE/nagios/tempest_purge.py -e production -s {site} --no-dry-run

    properties:
      - authorization:
          'anonymous':
            - job-read
            - job-discover
      - build-blocker:
          blocking-jobs:
            - "^tempest-nagios-site-{site}-.*"
            - "tempest-compute-host-check"
          block-level: 'GLOBAL'
          queue-scanning: 'ALL'
      - build-discarder:
          days-to-keep: 7


- job:
    name: 'tempest-cleanup'
    defaults: global
    node: tempest
    concurrent: false

    scm:
      - git:
         url: ssh://review.rc.nectar.org.au:29418/internal/nectar-testing.git
         credentials-id: 4946c3a5-9f5e-4eac-9ec4-90e1e348db14

    builders:
      - shell: |
          . /opt/tempest/bin/activate
          timeout 5m $WORKSPACE/nagios/tempest_purge.py -e production --no-dry-run

    properties:
      - authorization:
          'anonymous':
            - job-read
            - job-discover
      - build-blocker:
          blocking-jobs:
            - "^tempest-nagios-flavor-.*"
            - "^.*tempest-compute-host-check"
          block-level: 'GLOBAL'
          queue-scanning: 'ALL'


- job:
    name: 'tempest-cleanup-operator'
    defaults: global
    node: tempest
    concurrent: false

    scm:
      - git:
         url: ssh://review.rc.nectar.org.au:29418/internal/nectar-testing.git
         credentials-id: 4946c3a5-9f5e-4eac-9ec4-90e1e348db14

    builders:
      - shell: |
          . /opt/tempest/bin/activate
          timeout 5m $WORKSPACE/nagios/tempest_purge.py -e production -s tempest-operator --no-dry-run

    triggers:
      - timed: '30 2 * * *'

    properties:
      - authorization:
          'anonymous':
            - job-read
            - job-discover
      - build-blocker:
          blocking-jobs:
            - "^tempest-nagios-flavor-.*"
            - "^.*tempest-compute-host-check"
          block-level: 'GLOBAL'
          queue-scanning: 'ALL'
