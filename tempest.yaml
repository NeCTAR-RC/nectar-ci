- job:
    name: tempest-compute-host-check
    defaults: global
    node: tempest
    concurrent: true
    scm:
      - git:
         url: ssh://review.rc.nectar.org.au:29418/internal/nectar-testing.git
         credentials-id: cd8b8dd3-b897-4ecb-985d-180d5b6f8498
    builders:
      - shell: |
          #!/bin/bash
          echo
          echo "================================================================="
          echo
          echo "                   CLOUD:   $CLOUD"
          echo "                   AZ:      $AVAILABILITY_ZONE"
          echo "                   HOST:    $HOST"
          echo "                   STARTED: $(date)"
          echo
          echo "================================================================="
          echo
          . /opt/tempest/bin/activate
          tmpdir=`mktemp -d --suffix=_tempest`
          cd $WORKSPACE/tempest
          ./setup_tempest.py --host $HOST -s $AVAILABILITY_ZONE -e $CLOUD -j check-compute-host $tmpdir
          cd $tmpdir
          stestr run --serial --whitelist-file $WORKSPACE/tempest/whitelists/check-compute-host.yaml 2>&1 | grep --line-buffered -vE ' \w+Warning: |self._sock = None'
          RET=${PIPESTATUS[0]}
          rm -rf $tmpdir
          exit $RET
    parameters:
      - choice:
          name: 'CLOUD'
          choices:
            - production
            - testing
            - development
          description: "Cloud to run tempest on (Production, Test, Dev). Leave as Production if unsure."
      - choice:
          name: 'AVAILABILITY_ZONE'
          choices:
            - auckland
            - intersect-01
            - melbourne-qh2
            - melbourne-qh2-uom
            - monash-01
            - monash-02
            - monash-03
            - qld
            - swinburne-01
            - tasmania
            - tasmania-02
            - tasmania-s
            - coreservices
            - qh2-test
            - monash-test
            - aucklandtest
            - lani
            - luna
          description: 'Availability zone.'
      - string:
          name: 'HOST'
          description: |
                Nova host to test. <br>
                - Host must be in AVAILABILITY_ZONE; if not nova will return a 'No Valid Host' error.<br>
                - Leave blank to let scheduler choose a host.
    properties:
      - authorization:
          'nobody':
            - job-read
            - job-discover
    publishers:
      - trigger:
          project: 'tempest-cleanup-operator'
          threshold: 'FAILURE'

- job:
    name: tempest-compute-host-check-advanced
    defaults: global
    node: tempest
    concurrent: true
    scm:
      - git:
         url: ssh://review.rc.nectar.org.au:29418/internal/nectar-testing.git
         credentials-id: cd8b8dd3-b897-4ecb-985d-180d5b6f8498
    builders:
      - shell: |
          #!/bin/bash
          echo
          echo "================================================================="
          echo
          echo "                   CLOUD: $CLOUD"
          echo "                   AZ:    $AVAILABILITY_ZONE"
          echo "                   HOST:  $HOST"
          echo "                   TARGET_HOST: $TARGET_HOST"
          echo "                   BLOCK_MIGRATION: $BLOCK_MIGRATION"
          echo "                   STARTED: $(date)"
          echo
          echo "================================================================="
          echo
          . /opt/tempest/bin/activate
          tmpdir=`mktemp -d --suffix=_tempest`
          cd $WORKSPACE/tempest
          [ $BLOCK_MIGRATION = true ] && enable_block="--block_migration"
          ./setup_tempest.py --host $HOST --target_host $TARGET_HOST $enable_block -s $AVAILABILITY_ZONE -e $CLOUD -j check-compute-host-advanced $tmpdir
          cd $tmpdir
          stestr run --serial --whitelist-file $WORKSPACE/tempest/whitelists/check-compute-host-advanced.yaml 2>&1 | grep --line-buffered -vE ' \w+Warning: |self._sock = None'
          RET=${PIPESTATUS[0]}
          rm -rf $tmpdir
          exit $RET
    parameters:
      - choice:
          name: 'CLOUD'
          choices:
            - production
            - testing
            - development
          description: "Cloud to run tempest on (Production, Test, Dev). Leave as Production if unsure."
      - choice:
          name: 'AVAILABILITY_ZONE'
          choices:
            - auckland
            - intersect-01
            - melbourne-qh2
            - melbourne-qh2-uom
            - monash-01
            - monash-02
            - monash-03
            - qld
            - swinburne-01
            - tasmania
            - tasmania-02
            - tasmania-s
            - coreservices
            - qh2-test
            - monash-test
            - aucklandtest
            - lani
            - luna
          description: 'Availability zone.'
      - string:
          name: 'HOST'
          description: |
                Nova host to test. <br>
                - Host must be in AVAILABILITY_ZONE; if not nova will return a 'No Valid Host' error.<br>
                - Leave blank to let scheduler choose a host.
      - string:
          name: 'TARGET_HOST'
          description: |
                Nova target host used by migration tests. <br>
                - Host must be in AVAILABILITY_ZONE; if not nova will return a 'No Valid Host' error.<br>
                - Leave blank to let scheduler choose a host.
      - bool:
          name: 'BLOCK_MIGRATION'
          default: false
          description: "Whether to enable block migration during live migration. (Should enable it when no shared storage)"
    properties:
      - authorization:
          'nobody':
            - job-read
            - job-discover
    publishers:
      - trigger:
          project: 'tempest-cleanup-operator'
          threshold: 'FAILURE'

- job:
    name: tempest-cinder-check
    defaults: global
    node: tempest
    concurrent: true
    scm:
      - git:
         url: ssh://review.rc.nectar.org.au:29418/internal/nectar-testing.git
         credentials-id: cd8b8dd3-b897-4ecb-985d-180d5b6f8498
    builders:
      - shell: |
          #!/bin/bash
          . /opt/tempest/bin/activate
          tmpdir=`mktemp -d --suffix=_tempest`
          cd $WORKSPACE/tempest/
          ./setup_tempest.py -s $AVAILABILITY_ZONE -e $CLOUD -j check-cinder $tmpdir
          cd $tmpdir
          stestr run --serial --whitelist-file $WORKSPACE/tempest/whitelists/check-cinder.yaml 2>&1 | grep --line-buffered -vE ' \w+Warning: |self._sock = None'
          RET=${PIPESTATUS[0]}
          rm -rf $tmpdir
          exit $RET
    parameters:
      - choice:
          name: 'CLOUD'
          choices:
            - production
            - testing
            - development
          description: "Cloud to run tempest on (Production, Test, Dev). Leave as Production if unsure."
      - choice:
          name: 'AVAILABILITY_ZONE'
          choices:
            - auckland
            - intersect-01
            - melbourne-qh2
            - melbourne-qh2-uom
            - monash-01
            - monash-02
            - monash-03
            - qld
            - swinburne-01
            - tasmania
            - tasmania-02
            - tasmania-s
            - coreservices
            - qh2-test
            - monash-test
            - aucklandtest
            - lani
            - luna
          description: 'Availability zone.'
    properties:
      - authorization:
          'nobody':
            - job-read
            - job-discover

- job:
    name: tempest-magnum-check
    defaults: global
    node: tempest
    concurrent: true
    scm:
      - git:
         url: ssh://review.rc.nectar.org.au:29418/internal/nectar-testing.git
         credentials-id: cd8b8dd3-b897-4ecb-985d-180d5b6f8498
    builders:
      - shell: |
          #!/bin/bash
          . /opt/tempest/bin/activate
          tmpdir=`mktemp -d --suffix=_tempest`
          cd $WORKSPACE/tempest/
          ./setup_tempest.py -s $AVAILABILITY_ZONE -e $CLOUD -j check-magnum $tmpdir
          cd $tmpdir
          stestr run --serial --whitelist-file $WORKSPACE/tempest/whitelists/check-magnum.yaml 2>&1 | grep --line-buffered -vE ' \w+Warning: |self._sock = None'
          RET=${PIPESTATUS[0]}
          rm -rf $tmpdir
          exit $RET
    parameters:
      - choice:
          name: 'CLOUD'
          choices:
            - production
            - testing
            - development
          description: "Cloud to run tempest on (production, testing or development). Leave as Production if unsure."
      - choice:
          name: 'AVAILABILITY_ZONE'
          choices:
            - auckland
            - intersect-01
            - melbourne-qh2
            - melbourne-qh2-uom
            - monash-01
            - monash-02
            - monash-03
            - qld
            - swinburne-01
            - tasmania
            - tasmania-02
            - tasmania-s
            - coreservices
            - qh2-test
            - monash-test
            - aucklandtest
            - lani
            - luna
          description: 'Availability zone.'
    properties:
      - authorization:
          'nobody':
            - job-read
            - job-discover

- job:
    name: tempest-ec2api-check
    defaults: global
    node: tempest
    concurrent: true
    scm:
      - git:
         url: ssh://review.rc.nectar.org.au:29418/internal/nectar-testing.git
         credentials-id: cd8b8dd3-b897-4ecb-985d-180d5b6f8498
    builders:
      - shell: |
          #!/bin/bash
          . /opt/tempest/bin/activate
          tmpdir=`mktemp -d --suffix=_tempest`
          cd $WORKSPACE/tempest/
          ./setup_tempest.py -s $AVAILABILITY_ZONE -e $CLOUD -j check-ec2api $tmpdir
          ./setup_ec2api_image.py $tmpdir
          cd $tmpdir
          stestr run --serial --whitelist-file $WORKSPACE/tempest/whitelists/check-ec2api.yaml 2>&1 | grep --line-buffered -vE ' \w+Warning: |self._sock = None'
          RET=${PIPESTATUS[0]}
          rm -rf $tmpdir
          exit $RET
    parameters:
      - choice:
          name: 'CLOUD'
          choices:
            - production
            - testing
            - development
          description: "Cloud to run tempest on (production, testing or development). Leave as Production if unsure."
      - choice:
          name: 'AVAILABILITY_ZONE'
          choices:
            - auckland
            - intersect-01
            - melbourne-qh2
            - melbourne-qh2-uom
            - monash-01
            - monash-02
            - monash-03
            - qld
            - swinburne-01
            - tasmania
            - tasmania-02
            - tasmania-s
            - coreservices
            - qh2-test
            - monash-test
            - aucklandtest
            - lani
            - luna
          description: 'Availability zone.'
    properties:
      - authorization:
          'nobody':
            - job-read
            - job-discover


- project:
    name: tempest-s3-check
    jobs:
      - 'tempest-{name}-check-site-agnostic':
         whitelist: 's3'

- job:
    name: tempest-neutron-check
    defaults: global
    node: tempest
    concurrent: true
    scm:
      - git:
         url: ssh://review.rc.nectar.org.au:29418/internal/nectar-testing.git
         credentials-id: cd8b8dd3-b897-4ecb-985d-180d5b6f8498
    builders:
      - shell: |
          #!/bin/bash
          . /opt/tempest/bin/activate
          tmpdir=`mktemp -d --suffix=_tempest`
          cd $WORKSPACE/tempest/
          ./setup_tempest.py -e $CLOUD -s $AVAILABILITY_ZONE -j check-scenario $tmpdir
          cd $tmpdir
          stestr run --serial --whitelist-file $WORKSPACE/tempest/whitelists/check-neutron.yaml 2>&1 | grep --line-buffered -vE ' \w+Warning: |self._sock = None'
          RET=${PIPESTATUS[0]}
          rm -rf $tmpdir
          exit $RET
    parameters:
      - choice:
          name: 'CLOUD'
          choices:
            - production
            - testing
            - development
          description: "Cloud to run tempest on (production, testing or development). Leave as Production if unsure."
      - choice:
          name: 'AVAILABILITY_ZONE'
          choices:
            - auckland
            - intersect-01
            - melbourne-qh2
            - melbourne-qh2-uom
            - monash-01
            - monash-02
            - monash-03
            - qld
            - swinburne-01
            - tasmania
            - tasmania-02
            - tasmania-s
            - coreservices
            - qh2-test
            - monash-test
            - aucklandtest
            - lani
            - luna
          description: 'Availability zone.'
    properties:
      - authorization:
          'nobody':
            - job-read
            - job-discover

- project:
    name: neutron-upgrade
    jobs:
      - 'tempest-{name}-check':
         whitelist: 'upgrade-neutron'

- job:
    name: tempest-scenario-check
    defaults: global
    node: tempest
    concurrent: true
    scm:
      - git:
         url: ssh://review.rc.nectar.org.au:29418/internal/nectar-testing.git
         credentials-id: cd8b8dd3-b897-4ecb-985d-180d5b6f8498
    builders:
      - shell: |
          #!/bin/bash
          echo
          echo "================================================================="
          echo
          echo "                   CLOUD:   $CLOUD"
          echo "                   AZ:      $AVAILABILITY_ZONE"
          echo "                   HOST:    $HOST"
          echo "                   STARTED: $(date)"
          echo
          echo "================================================================="
          echo
          . /opt/tempest/bin/activate
          tmpdir=`mktemp -d --suffix=_tempest`
          cd $WORKSPACE/tempest/
          ./setup_tempest.py --host $HOST -s $AVAILABILITY_ZONE -e $CLOUD -j check-scenario $tmpdir
          cd $tmpdir
          stestr run --serial --whitelist-file $WORKSPACE/tempest/whitelists/check-scenario.yaml 2>&1 | grep --line-buffered -vE ' \w+Warning: |self._sock = None'
          RET=${PIPESTATUS[0]}
          rm -rf $tmpdir
          exit $RET
    parameters:
      - choice:
          name: 'CLOUD'
          choices:
            - production
            - testing
            - development
          description: "Cloud to run tempest on (Production, Test, Dev). Leave as Production if unsure."
      - choice:
          name: 'AVAILABILITY_ZONE'
          choices:
            - auckland
            - intersect-01
            - melbourne-qh2
            - melbourne-qh2-uom
            - monash-01
            - monash-02
            - monash-03
            - qld
            - swinburne-01
            - tasmania
            - tasmania-02
            - tasmania-s
            - coreservices
            - qh2-test
            - monash-test
            - aucklandtest
            - lani
            - luna
          description: 'Availability zone.'
      - string:
          name: 'HOST'
          description: |
                Nova host to test. <br>
                - Host must be in AVAILABILITY_ZONE; if not nova will return a 'No Valid Host' error.<br>
                - Leave blank to let scheduler choose a host.
    properties:
      - authorization:
          'nobody':
            - job-read
            - job-discover

- job:
    name: tempest-murano-check
    defaults: global
    node: tempest
    concurrent: true
    scm:
      - git:
         url: ssh://review.rc.nectar.org.au:29418/internal/nectar-testing.git
         credentials-id: cd8b8dd3-b897-4ecb-985d-180d5b6f8498
    builders:
      - shell: |
          #!/bin/bash
          . /opt/tempest/bin/activate
          tmpdir=`mktemp -d --suffix=_tempest`
          cd $WORKSPACE/tempest/
          ./setup_tempest.py -s $AVAILABILITY_ZONE -e $CLOUD -j check-murano $tmpdir
          cd $tmpdir
          stestr run -n murano_tempest_tests.tests.scenario.application_catalog.test_deployment.TestMuranoDeployment.test_app_deployment 2>&1 | grep --line-buffered -vE ' \w+Warning: |self._sock = None'
          RET=${PIPESTATUS[0]}
          rm -rf $tmpdir
          exit $RET
    parameters:
      - choice:
          name: 'CLOUD'
          choices:
            - production
            - testing
            - development
          description: "Cloud to run tempest on (Production, Test, Dev). Leave as Production if unsure."
      - choice:
          name: 'AVAILABILITY_ZONE'
          choices:
            - auckland
            - intersect-01
            - melbourne-qh2
            - melbourne-qh2-uom
            - monash-01
            - monash-02
            - monash-03
            - qld
            - swinburne-01
            - tasmania
            - tasmania-02
            - tasmania-s
            - coreservices
            - qh2-test
            - monash-test
            - aucklandtest
            - lani
            - luna
          description: 'Availability zone.'
    properties:
      - authorization:
          'nobody':
            - job-read
            - job-discover


- job:
    name: tempest-trove-check
    defaults: global
    node: tempest
    concurrent: true
    scm:
      - git:
         url: ssh://review.rc.nectar.org.au:29418/internal/nectar-testing.git
         credentials-id: cd8b8dd3-b897-4ecb-985d-180d5b6f8498
    builders:
      - shell: |
          #!/bin/bash
          . /opt/tempest/bin/activate
          tmpdir=`mktemp -d --suffix=_tempest`
          cd $WORKSPACE/tempest/
          ./setup_tempest.py -s $AVAILABILITY_ZONE -e $CLOUD -j check-trove $tmpdir
          cd $tmpdir
          stestr run -n trove_tempest_plugin.tests.scenario.database.test_basic_instance_ops.DatabaseScenarioTest.test_create_instances 2>&1 | grep --line-buffered -vE ' \w+Warning: |self._sock = None'
          RET=${PIPESTATUS[0]}
          rm -rf $tmpdir
          exit $RET
    parameters:
      - choice:
          name: 'CLOUD'
          choices:
            - production
            - testing
            - development
          description: "Cloud to run tempest on (Production, Test, Dev). Leave as Production if unsure."
      - choice:
          name: 'AVAILABILITY_ZONE'
          choices:
            - auckland
            - intersect-01
            - melbourne-qh2
            - melbourne-qh2-uom
            - monash-01
            - monash-02
            - monash-03
            - qld
            - swinburne-01
            - tasmania
            - tasmania-02
            - tasmania-s
            - coreservices
            - qh2-test
            - monash-test
            - aucklandtest
            - lani
            - luna
          description: 'Availability zone.'
    properties:
      - authorization:
          'nobody':
            - job-read
            - job-discover


- job:
    name: tempest-blazar-check
    defaults: global
    node: tempest
    concurrent: true
    scm:
      - git:
         url: ssh://review.rc.nectar.org.au:29418/internal/nectar-testing.git
         credentials-id: cd8b8dd3-b897-4ecb-985d-180d5b6f8498
    builders:
      - shell: |
          #!/bin/bash
          . /opt/tempest/bin/activate
          tmpdir=`mktemp -d --suffix=_tempest`
          cd $WORKSPACE/tempest
          ./setup_tempest.py -s $AVAILABILITY_ZONE -e $CLOUD --blazar-resource-properties "$BLAZAR_RESOURCE_PROPERTIES" --blazar-extra-specs "$BLAZAR_EXTRA_SPECS" -j check-blazar $tmpdir
          cd $tmpdir
          stestr run --serial --whitelist-file $WORKSPACE/tempest/whitelists/check-blazar.yaml 2>&1 | grep --line-buffered -vE ' \w+Warning: |self._sock = None'
          RET=${PIPESTATUS[0]}
          rm -rf $tmpdir
          exit $RET
    parameters:
      - choice:
          name: 'CLOUD'
          choices:
            - production
            - testing
            - development
          description: "Cloud to run tempest on (Production, Test, Dev). Leave as Production if unsure."
      - choice:
          name: 'AVAILABILITY_ZONE'
          choices:
            - auckland
            - intersect-01
            - melbourne-qh2
            - melbourne-qh2-uom
            - monash-01
            - monash-02
            - monash-03
            - qld
            - swinburne-01
            - tasmania
            - tasmania-02
            - tasmania-s
            - coreservices
            - qh2-test
            - monash-test
            - aucklandtest
            - lani
            - luna
          description: 'Availability zone.'
      - string:
          name: 'BLAZAR_RESOURCE_PROPERTIES'
          description: |
                Optional. Specify Blazar resource_properties. <br>
                - e.g. To test creating a lease on a host in Auckland zone with a custom capability, specify <br>
                - '["and", ["=", "\$$availability_zone", "auckland"], ["=", "\$$custom_key", "custom_value"]]'
      - string:
          name: 'BLAZAR_EXTRA_SPECS'
          description: |
                Optional. Specify Blazar extra_specs. <br>
                - e.g. To test creating a lease for a P40 GPU, specify <br>
                - '{"pci_passthrough:alias":"P40:1"}'
    properties:
      - authorization:
          'nobody':
            - job-read
            - job-discover
    publishers:
      - trigger:
          project: 'tempest-cleanup-operator'
          threshold: 'FAILURE'


- project:
    name: trove-mysql
    jobs:
      - 'tempest-{name}-check':
         whitelist: 'trove'

- project:
    name: trove-postgresql
    jobs:
      - 'tempest-{name}-check':
         whitelist: 'trove'

- project:
    name: trove-mysql-5.7
    jobs:
      - 'tempest-{name}-check':
         whitelist: 'trove'

- project:
    name: trove-mysql-8.0
    jobs:
      - 'tempest-{name}-check':
         whitelist: 'trove'

- project:
    name: trove-postgresql-9.6
    jobs:
      - 'tempest-{name}-check':
         whitelist: 'trove'

- project:
    name: heat-scenario
    jobs:
      - 'tempest-{name}-check':
         whitelist: 'heat'

- project:
    name: octavia-smoke
    jobs:
      - 'tempest-{name}-check':
         whitelist: 'octavia'

- project:
    name: octavia-smoke-rctest
    jobs:
      - 'tempest-{name}-check-{site}':
         job: 'octavia-smoke'
         whitelist: 'octavia'
         cloud: 'testing'
         site:
           - coreservices

- project:
    name: s3-rctest
    jobs:
      - 'tempest-{name}-check-site-agnostic-{cloud}':
         job: 's3'
         whitelist: 's3'
         cloud: 'testing'

- project:
    name: heat-scenario-rctest
    jobs:
      - 'tempest-{name}-check-{site}':
         job: 'heat-scenario'
         whitelist: 'heat'
         cloud: 'testing'
         site:
           - coreservices
           - qh2-test
           - monash-test
           - aucklandtest

- project:
    name: manila-scenario
    jobs:
      - 'tempest-{name}-check':
          whitelist: 'manila'

- project:
    name: manila-nfs
    jobs:
      - 'tempest-{name}-check':
          whitelist: 'manila-nfs'

- project:
    name: manila-cephfs
    jobs:
      - 'tempest-{name}-check':
          whitelist: 'manila-cephfs'

- project:
    name: manila-cephfs-rctest
    jobs:
      - 'tempest-{name}-check-{site}':
         job: 'manila-cephfs'
         whitelist: 'manila-cephfs'
         cloud: 'testing'
         site:
           - coreservices

- project:
    name: tempest-nagios-services-manila-nfs
    site:
      - auckland
    jobs:
      - 'tempest-nagios-services-{site}-{test}':
          test:
            - 'share_nfs'
          skip_capacity_check: true
          environment: 'production'

- project:
    name: tempest-nagios-services-manila-cephfs
    site:
      - monash-02
    jobs:
      - 'tempest-nagios-services-{site}-{test}':
          test:
            - 'share_cephfs'
          skip_capacity_check: true
          environment: 'production'

- project:
    name: tempest-nagios-services-manila-cephfs-rctest
    site:
      - coreservice
    jobs:
      - 'tempest-nagios-services-{site}-{test}':
          test:
            - 'share_cephfs'
          skip_capacity_check: true
          environment: 'testing'

- project:
    name: tempest-nagios-services
    site:
      - auckland
      - intersect-01
      - melbourne-qh2
      - melbourne-qh2-uom
      - monash-01
      - monash-02
      - qld
      - swinburne-01
      - tasmania
      - tasmania-02
      - tasmania-s
    jobs:
      - 'tempest-nagios-services-{site}-{test}':
          test:
            - 'volume'
            - 'orchestration'
            - 'database'
            - 'application'
            - 'ec2api'
          skip_capacity_check: false
          environment: 'production'

- project:
    name: tempest-nagios-services-volume
    site:
      - monash-03
      - restricted-qld
    jobs:
      - 'tempest-nagios-services-{site}-{test}':
          test:
            - 'volume'
          skip_capacity_check: false
          environment: 'production'

- project:
    name: tempest-nagios-services-volume-backup
    site:
      - auckland
      - melbourne-qh2
      - melbourne-qh2-uom
      - monash-01
      - monash-02
      - monash-03
      - qld
      - swinburne-01
      - tasmania
      - tasmania-02
      - tasmania-s
    jobs:
      - 'tempest-nagios-services-{site}-{test}':
          test:
            - 'volume_backup'
          skip_capacity_check: false
          environment: 'production'

- project:
    name: tempest-nagios-services-advanced-network
    site:
      - auckland
      - intersect-01
      - melbourne-qh2
      - monash-01
      - monash-02
      - qld
      - swinburne-01
      - tasmania
    jobs:
      - 'tempest-nagios-services-{site}-{test}':
          test:
            - 'loadbalancer'
          skip_capacity_check: false
          environment: 'production'

- project:
    name: tempest-nagios-services-rctest
    site:
      - coreservices
      - qh2-test
      - monash-test
      - aucklandtest
    jobs:
      - 'tempest-nagios-services-{site}-{test}':
          test:
            - 'volume'
            - 'volume_backup'
            - 'orchestration'
            - 'loadbalancer'
            - 'database'
            - 'coe'
            - 'application'
            - 'ec2api'
          skip_capacity_check: false
          environment: 'testing'


- project:
    name: tempest-nagios-services-site-agnostic
    environment: 'production'
    jobs:
      - 'tempest-nagios-services-{test}-{environment}':
          test:
            - 'image'
            - 'object'
            - 'dns'
          skip_capacity_check: true

- project:
    name: tempest-nagios-services-site-agnostic-rctest
    environment: 'testing'
    jobs:
      - 'tempest-nagios-services-{test}-{environment}':
          test:
            - 'image'
            - 'object'
            - 'dns'
          skip_capacity_check: true

- project:
    name: telemetry
    jobs:
      - 'tempest-{name}-check':
          whitelist: 'telemetry'

- project:
    name: telemetry-rctest
    cloud: 'testing'
    jobs:
      - 'tempest-{name}-check-{site}':
          job: 'telemetry'
          whitelist: 'telemetry'
          site:
            - coreservices
      - 'tempest-cleanup-telemetry-{cloud}'

- project:
    name: glance-scenario
    jobs:
      - 'tempest-{name}-check-site-agnostic':
         whitelist: 'glance'

- project:
    name: glance-scenario-rctest
    jobs:
      - 'tempest-{name}-check-site-agnostic-{cloud}':
         job: 'glance-scenario'
         whitelist: 'glance'
         cloud: 'testing'

- project:
    name: swift-scenario
    jobs:
      - 'tempest-{name}-check-site-agnostic':
         whitelist: 'swift'

- project:
    name: swift-scenario-rctest
    jobs:
      - 'tempest-{name}-check-site-agnostic-{cloud}':
         job: 'swift-scenario'
         whitelist: 'swift'
         cloud: 'testing'

- job:
    name: tempest-designate-check
    defaults: global
    node: tempest
    concurrent: true
    scm:
      - git:
         url: ssh://review.rc.nectar.org.au:29418/internal/nectar-testing.git
         credentials-id: cd8b8dd3-b897-4ecb-985d-180d5b6f8498
    builders:
      - shell: |
          #!/bin/bash
          . /opt/tempest/bin/activate
          tmpdir=`mktemp -d --suffix=_tempest`
          cd $WORKSPACE/tempest/
          ./setup_tempest.py -e $CLOUD -j check-designate $tmpdir
          cd $tmpdir
          stestr run -n designate_tempest_plugin.tests.scenario.v2.test_recordsets.RecordsetsTest 2>&1 | grep --line-buffered -vE ' \w+Warning: |self._sock = None'
          RET=${PIPESTATUS[0]}
          rm -rf $tmpdir
          exit $RET
    parameters:
      - choice:
          name: 'CLOUD'
          choices:
            - production
            - testing
            - development
          description: "Cloud to run tempest on (Production, Test, Dev). Leave as Production if unsure."
    properties:
      - authorization:
          'nobody':
            - job-read
            - job-discover


- job:
    name: tempest-rctest-compute-host-check
    defaults: global
    node: tempest
    concurrent: true
    scm:
      - git:
         url: ssh://review.rc.nectar.org.au:29418/internal/nectar-testing.git
         credentials-id: cd8b8dd3-b897-4ecb-985d-180d5b6f8498
    builders:
      - shell: |
          #!/bin/bash
          echo
          echo "================================================================="
          echo
          echo "                   CLOUD:   testing"
          echo "                   AZ:      coreservices"
          echo "                   STARTED: $(date)"
          echo
          echo "================================================================="
          echo
          . /opt/tempest/bin/activate
          tmpdir=`mktemp -d --suffix=_tempest`
          cd $WORKSPACE/tempest
          ./setup_tempest.py -s coreservices -e testing -j check-compute-host $tmpdir
          cd $tmpdir
          stestr run --serial --whitelist-file $WORKSPACE/tempest/whitelists/check-compute-host.yaml 2>&1 | grep --line-buffered -vE ' \w+Warning: |self._sock = None'
          RET=${PIPESTATUS[0]}
          rm -rf $tmpdir
          exit $RET
    properties:
      - authorization:
          'nobody':
            - job-read
            - job-discover
      - throttle:
          categories:
            - tempest-rctest
          option: category


- job:
    name: tempest-rctest-compute-host-check-advanced
    defaults: global
    node: tempest
    concurrent: true
    scm:
      - git:
         url: ssh://review.rc.nectar.org.au:29418/internal/nectar-testing.git
         credentials-id: cd8b8dd3-b897-4ecb-985d-180d5b6f8498
    builders:
      - shell: |
          #!/bin/bash
          echo
          echo "================================================================="
          echo
          echo "                   CLOUD:   testing"
          echo "                   AZ:      coreservices"
          echo "                   STARTED: $(date)"
          echo
          echo "================================================================="
          echo
          . /opt/tempest/bin/activate
          tmpdir=`mktemp -d --suffix=_tempest`
          cd $WORKSPACE/tempest
          ./setup_tempest.py -s coreservices -e testing -j check-compute-host-advanced --block_migration $tmpdir
          cd $tmpdir
          stestr run --serial --whitelist-file $WORKSPACE/tempest/whitelists/check-compute-host-advanced.yaml 2>&1 | grep --line-buffered -vE ' \w+Warning: |self._sock = None'
          RET=${PIPESTATUS[0]}
          rm -rf $tmpdir
          exit $RET
    properties:
      - authorization:
          'nobody':
            - job-read
            - job-discover


- job:
    name: tempest-rctest-cinder-check
    defaults: global
    node: tempest
    concurrent: true
    scm:
      - git:
         url: ssh://review.rc.nectar.org.au:29418/internal/nectar-testing.git
         credentials-id: cd8b8dd3-b897-4ecb-985d-180d5b6f8498
    builders:
      - shell: |
          #!/bin/bash
          . /opt/tempest/bin/activate
          tmpdir=`mktemp -d --suffix=_tempest`
          cd $WORKSPACE/tempest/
          ./setup_tempest.py -s coreservices -e testing -j check-cinder $tmpdir
          cd $tmpdir
          stestr run --serial --whitelist-file $WORKSPACE/tempest/whitelists/check-cinder.yaml 2>&1 | grep --line-buffered -vE ' \w+Warning: |self._sock = None'
          RET=${PIPESTATUS[0]}
          rm -rf $tmpdir
          exit $RET
    properties:
      - authorization:
          'nobody':
            - job-read
            - job-discover
      - throttle:
          categories:
            - tempest-rctest
          option: category


- job:
    name: tempest-rctest-magnum-check
    defaults: global
    node: tempest
    concurrent: true
    scm:
      - git:
         url: ssh://review.rc.nectar.org.au:29418/internal/nectar-testing.git
         credentials-id: cd8b8dd3-b897-4ecb-985d-180d5b6f8498
    builders:
      - shell: |
          #!/bin/bash
          . /opt/tempest/bin/activate
          tmpdir=`mktemp -d --suffix=_tempest`
          cd $WORKSPACE/tempest/
          ./setup_tempest.py -s coreservices -e testing -j check-magnum $tmpdir
          cd $tmpdir
          stestr run --serial --whitelist-file $WORKSPACE/tempest/whitelists/check-magnum.yaml 2>&1 | grep --line-buffered -vE ' \w+Warning: |self._sock = None'
          RET=${PIPESTATUS[0]}
          rm -rf $tmpdir
          exit $RET
    properties:
      - authorization:
          'nobody':
            - job-read
            - job-discover
      - throttle:
          categories:
            - tempest-rctest
          option: category


- job:
    name: tempest-rctest-ec2api-check
    defaults: global
    node: tempest
    concurrent: true
    scm:
      - git:
         url: ssh://review.rc.nectar.org.au:29418/internal/nectar-testing.git
         credentials-id: cd8b8dd3-b897-4ecb-985d-180d5b6f8498
    builders:
      - shell: |
          #!/bin/bash
          . /opt/tempest/bin/activate
          tmpdir=`mktemp -d --suffix=_tempest`
          cd $WORKSPACE/tempest/
          ./setup_tempest.py -s coreservices -e testing -j check-ec2api $tmpdir
          ./setup_ec2api_image.py $tmpdir
          cd $tmpdir
          stestr run --serial --whitelist-file $WORKSPACE/tempest/whitelists/check-ec2api.yaml 2>&1 | grep --line-buffered -vE ' \w+Warning: |self._sock = None'
          RET=${PIPESTATUS[0]}
          rm -rf $tmpdir
          exit $RET
    properties:
      - authorization:
          'nobody':
            - job-read
            - job-discover
      - throttle:
          categories:
            - tempest-rctest
          option: category


- job:
    name: tempest-rctest-neutron-check
    defaults: global
    node: tempest
    concurrent: true
    scm:
      - git:
         url: ssh://review.rc.nectar.org.au:29418/internal/nectar-testing.git
         credentials-id: cd8b8dd3-b897-4ecb-985d-180d5b6f8498
    builders:
      - shell: |
          #!/bin/bash
          . /opt/tempest/bin/activate
          tmpdir=`mktemp -d --suffix=_tempest`
          cd $WORKSPACE/tempest/
          ./setup_tempest.py -s coreservices -e testing -j check-scenario $tmpdir
          cd $tmpdir
          stestr run --serial --whitelist-file $WORKSPACE/tempest/whitelists/check-neutron.yaml 2>&1 | grep --line-buffered -vE ' \w+Warning: |self._sock = None'
          RET=${PIPESTATUS[0]}
          rm -rf $tmpdir
          exit $RET
    properties:
      - authorization:
          'nobody':
            - job-read
            - job-discover
      - throttle:
          categories:
            - tempest-rctest
          option: category


- job:
    name: tempest-rctest-scenario-check
    defaults: global
    node: tempest
    concurrent: true
    scm:
      - git:
         url: ssh://review.rc.nectar.org.au:29418/internal/nectar-testing.git
         credentials-id: cd8b8dd3-b897-4ecb-985d-180d5b6f8498
    builders:
      - shell: |
          #!/bin/bash
          . /opt/tempest/bin/activate
          tmpdir=`mktemp -d --suffix=_tempest`
          cd $WORKSPACE/tempest/
          ./setup_tempest.py -s coreservices -e testing -j check-scenario $tmpdir
          cd $tmpdir
          stestr run --serial --whitelist-file $WORKSPACE/tempest/whitelists/check-scenario.yaml 2>&1 | grep --line-buffered -vE ' \w+Warning: |self._sock = None'
          RET=${PIPESTATUS[0]}
          rm -rf $tmpdir
          exit $RET
    properties:
      - authorization:
          'nobody':
            - job-read
            - job-discover
      - throttle:
          categories:
            - tempest-rctest
          option: category


- job:
    name: tempest-rctest-murano-check
    defaults: global
    node: tempest
    concurrent: true
    scm:
      - git:
         url: ssh://review.rc.nectar.org.au:29418/internal/nectar-testing.git
         credentials-id: cd8b8dd3-b897-4ecb-985d-180d5b6f8498
    builders:
      - shell: |
          #!/bin/bash
          . /opt/tempest/bin/activate
          tmpdir=`mktemp -d --suffix=_tempest`
          cd $WORKSPACE/tempest/
          ./setup_tempest.py -s coreservices -e testing -j check-murano $tmpdir
          cd $tmpdir
          stestr run -n murano_tempest_tests.tests.scenario.application_catalog.test_deployment.TestMuranoDeployment.test_app_deployment 2>&1 | grep --line-buffered -vE ' \w+Warning: |self._sock = None'
          RET=${PIPESTATUS[0]}
          rm -rf $tmpdir
          exit $RET
    properties:
      - authorization:
          'nobody':
            - job-read
            - job-discover
      - throttle:
          categories:
            - tempest-rctest
          option: category


- job:
    name: tempest-rctest-trove-check
    defaults: global
    node: tempest
    concurrent: true
    scm:
      - git:
         url: ssh://review.rc.nectar.org.au:29418/internal/nectar-testing.git
         credentials-id: cd8b8dd3-b897-4ecb-985d-180d5b6f8498
    builders:
      - shell: |
          #!/bin/bash
          . /opt/tempest/bin/activate
          tmpdir=`mktemp -d --suffix=_tempest`
          cd $WORKSPACE/tempest/
          ./setup_tempest.py -s coreservices -e testing -j check-trove $tmpdir
          cd $tmpdir
          stestr run -n trove_tempest_plugin.tests.scenario.database.test_basic_instance_ops.DatabaseScenarioTest.test_create_instances 2>&1 | grep --line-buffered -vE ' \w+Warning: |self._sock = None'
          RET=${PIPESTATUS[0]}
          rm -rf $tmpdir
          exit $RET
    properties:
      - authorization:
          'nobody':
            - job-read
            - job-discover
      - throttle:
          categories:
            - tempest-rctest
          option: category


- job:
    name: tempest-rctest-designate-check
    defaults: global
    node: tempest
    concurrent: true
    scm:
      - git:
         url: ssh://review.rc.nectar.org.au:29418/internal/nectar-testing.git
         credentials-id: cd8b8dd3-b897-4ecb-985d-180d5b6f8498
    builders:
      - shell: |
          #!/bin/bash
          . /opt/tempest/bin/activate
          tmpdir=`mktemp -d --suffix=_tempest`
          cd $WORKSPACE/tempest/
          ./setup_tempest.py -e testing -j check-designate $tmpdir
          cd $tmpdir
          stestr run -n designate_tempest_plugin.tests.scenario.v2.test_recordsets.RecordsetsTest 2>&1 | grep --line-buffered -vE ' \w+Warning: |self._sock = None'
          RET=${PIPESTATUS[0]}
          rm -rf $tmpdir
          exit $RET
    properties:
      - authorization:
          'nobody':
            - job-read
            - job-discover
      - throttle:
          categories:
            - tempest-rctest
          option: category


- project:
    name: tempest-nagios-site-compute
    site:
        - auckland
        - intersect-01
        - melbourne-qh2
        - melbourne-qh2-uom
        - monash-01
        - monash-02
        - monash-03
        - qld
        - swinburne-01
        - tasmania
        - tasmania-02
        - tasmania-s
    jobs:
      - 'tempest-nagios-site-{site}-{test}':
          test:
            - 'compute'
      - 'tempest-cleanup-site-{site}'

- project:
    name: tempest-nagios-site-network
    site:
        - auckland
        - intersect-01
        - melbourne-qh2
        - monash-01
        - monash-02
        - qld
        - restricted-qld
        - swinburne-01
        - tasmania
    jobs:
      - 'tempest-nagios-site-{site}-{test}':
          test:
            - 'network'

- project:
    name: tempest-nagios-flavor
    flavor:
        - xsmall
        - small
        - medium
        - large
        - xlarge
        - xxlarge
    jobs:
      - 'tempest-nagios-flavor-{flavor}-{test}':
          test:
            - 'compute'


- job-template:
    name: 'tempest-nagios-site-{site}-{test}'
    defaults: global
    node: tempest
    concurrent: false

    scm:
      - git:
         url: ssh://review.rc.nectar.org.au:29418/internal/nectar-testing.git
         credentials-id: cd8b8dd3-b897-4ecb-985d-180d5b6f8498

    wrappers:
      - timeout:
          timeout: 10
          fail: true

    builders:
      - shell: |
          . /opt/tempest/bin/activate
          $WORKSPACE/nagios/tempest_nagios.py tempest -c $WORKSPACE/nagios/tempest_nagios.conf -s {site} -t {test} -e production

    triggers:
      - timed: 'H/30 * * * *'

    publishers:
      - naginator:
          max-failed-builds: 2
          fixed-delay: 30
      - trigger:
          project: 'tempest-cleanup-site-{site}'
          threshold: 'FAILURE'

    properties:
      - authorization:
          'anonymous':
            - job-read
            - job-discover
      - build-discarder:
          days-to-keep: 7


- job-template:
    name: 'tempest-nagios-flavor-{flavor}-{test}'
    defaults: global
    node: tempest
    concurrent: false

    scm:
      - git:
         url: ssh://review.rc.nectar.org.au:29418/internal/nectar-testing.git
         credentials-id: cd8b8dd3-b897-4ecb-985d-180d5b6f8498

    wrappers:
      - timeout:
          timeout: 45
          fail: true

    builders:
      - shell: |
          . /opt/tempest/bin/activate
          $WORKSPACE/nagios/tempest_nagios.py tempest -c $WORKSPACE/nagios/tempest_nagios.conf -f {flavor} -t {test} -e production

    triggers:
      - timed: 'H * * * *'

    publishers:
      - naginator:
          max-failed-builds: 2
          fixed-delay: 30
      - trigger:
          project: 'tempest-cleanup'
          threshold: 'FAILURE'

    properties:
      - authorization:
          'anonymous':
            - job-read
            - job-discover
      - build-discarder:
          days-to-keep: 7

- job-template:
    name: 'tempest-nagios-services-{site}-{test}'
    defaults: global
    node: tempest
    concurrent: false

    scm:
      - git:
         url: ssh://review.rc.nectar.org.au:29418/internal/nectar-testing.git
         credentials-id: cd8b8dd3-b897-4ecb-985d-180d5b6f8498

    wrappers:
      - timeout:
          timeout: 45
          fail: true

    builders:
      - shell: |
          . /opt/tempest/bin/activate
          [ {skip_capacity_check} == True ] && skip_capacity_check="--skip-capacity-check"
          $WORKSPACE/nagios/tempest_nagios.py tempest -c $WORKSPACE/nagios/tempest_nagios.conf -s {site} -t {test} -e {environment} $skip_capacity_check

    triggers:
      - timed: 'H H(4-6) * * *'

    publishers:
      - naginator:
          max-failed-builds: 1
          fixed-delay: 30
      - trigger:
          project: 'tempest-cleanup-site-{site}'
          threshold: 'FAILURE'

    properties:
      - authorization:
          'anonymous':
            - job-read
            - job-discover
      - build-discarder:
          days-to-keep: 7
      - build-blocker:
          blocking-jobs:
            - "^tempest-nagios-site-{site}.*"
            - "^tempest-nagios-services-{site}.*"
          block-level: 'GLOBAL'

- job-template:
    name: 'tempest-nagios-services-{test}-{environment}'
    defaults: global
    node: tempest
    concurrent: false

    scm:
      - git:
         url: ssh://review.rc.nectar.org.au:29418/internal/nectar-testing.git
         credentials-id: cd8b8dd3-b897-4ecb-985d-180d5b6f8498

    wrappers:
      - timeout:
          timeout: 45
          fail: true

    builders:
      - shell: |
          . /opt/tempest/bin/activate
          [ {skip_capacity_check} == True ] && skip_capacity_check="--skip-capacity-check"
          $WORKSPACE/nagios/tempest_nagios.py tempest -c $WORKSPACE/nagios/tempest_nagios.conf -t {test} -e {environment} $skip_capacity_check

    triggers:
      - timed: 'H H(3-4) * * *'

    publishers:
      - naginator:
          max-failed-builds: 1
          fixed-delay: 30
      - trigger:
          project: 'tempest-cleanup'
          threshold: 'FAILURE'

    properties:
      - authorization:
          'anonymous':
            - job-read
            - job-discover
      - build-discarder:
          days-to-keep: 7


- job-template:
    name: 'tempest-cleanup-site-{site}'
    defaults: global
    node: tempest
    concurrent: false

    scm:
      - git:
         url: ssh://review.rc.nectar.org.au:29418/internal/nectar-testing.git
         credentials-id: cd8b8dd3-b897-4ecb-985d-180d5b6f8498

    wrappers:
      - timeout:
          timeout: 5
          fail: true

    builders:
      - shell: |
          . /opt/tempest/bin/activate
          $WORKSPACE/nagios/tempest_purge.py -e production -s {site} --no-dry-run

    properties:
      - authorization:
          'anonymous':
            - job-read
            - job-discover
      - build-blocker:
          blocking-jobs:
            - "^tempest-nagios-site-{site}-.*"
            - "^tempest-nagios-services-{site}.*"
          block-level: 'GLOBAL'
      - build-discarder:
          days-to-keep: 7


- job-template:
    name: 'tempest-cleanup-telemetry-{cloud}'
    defaults: global
    node: tempest
    concurrent: false

    scm:
      - git:
         url: ssh://review.rc.nectar.org.au:29418/internal/nectar-testing.git
         credentials-id: cd8b8dd3-b897-4ecb-985d-180d5b6f8498

    wrappers:
      - timeout:
          timeout: 5
          fail: true

    triggers:
      - timed: '30 3 * * *'

    builders:
      - shell: |
          . /opt/tempest/bin/activate
          $WORKSPACE/nagios/tempest_purge.py -e {cloud} -j check-telemetry --no-dry-run

    properties:
      - authorization:
          'anonymous':
            - job-read
            - job-discover
      - build-blocker:
          blocking-jobs:
            - "^tempest-telemetry-.*"
          block-level: 'GLOBAL'
      - build-discarder:
          days-to-keep: 7


- job:
    name: 'tempest-cleanup'
    defaults: global
    node: tempest
    concurrent: false

    scm:
      - git:
         url: ssh://review.rc.nectar.org.au:29418/internal/nectar-testing.git
         credentials-id: cd8b8dd3-b897-4ecb-985d-180d5b6f8498

    wrappers:
      - timeout:
          timeout: 5
          fail: true

    builders:
      - shell: |
          . /opt/tempest/bin/activate
          $WORKSPACE/nagios/tempest_purge.py -e production --no-dry-run

    properties:
      - authorization:
          'anonymous':
            - job-read
            - job-discover
      - build-blocker:
          blocking-jobs:
            - "^tempest-nagios-flavor-.*"
          block-level: 'GLOBAL'
          queue-scanning: 'ALL'
      - build-discarder:
          days-to-keep: 7


- job:
    name: 'tempest-cleanup-operator'
    defaults: global
    node: tempest
    concurrent: false

    scm:
      - git:
         url: ssh://review.rc.nectar.org.au:29418/internal/nectar-testing.git
         credentials-id: cd8b8dd3-b897-4ecb-985d-180d5b6f8498

    wrappers:
      - timeout:
          timeout: 5
          fail: true

    builders:
      - shell: |
          . /opt/tempest/bin/activate
          $WORKSPACE/nagios/tempest_purge.py -e production -j operator --no-dry-run

    triggers:
      - timed: '30 2 * * *'

    properties:
      - authorization:
          'anonymous':
            - job-read
            - job-discover
      - build-blocker:
          blocking-jobs:
            - "^.*tempest-compute-host-check"
            - "^.*tempest-compute-host-check-advanced"
            - "tempest-magnum-check"
          block-level: 'GLOBAL'


- job:
    name: tempest-image-check-run-all
    defaults: global
    node: tempest
    parameters:
      - choice:
          name: 'AVAILABILITY_ZONE'
          description: 'Availability zone. Use "Any zone" to allow the scheduler to choose.'
          choices:
            - Any zone
            - auckland
            - intersect-01
            - melbourne-qh2
            - melbourne-qh2-uom
            - monash-01
            - monash-02
            - monash-03
            - qld
            - swinburne-01
            - tasmania
            - tasmania-02
            - tasmania-s
    builders:
      - shell: |
          #!/bin/bash -eu
          export OS_USERNAME=$OS_USERNAME
          export OS_PASSWORD=$OS_PASSWORD
          export OS_AUTH_URL=https://keystone.rc.nectar.org.au:5000/v3
          export OS_PROJECT_DOMAIN_NAME=Default
          export OS_USER_DOMAIN_NAME=Default
          export OS_IDENTITY_API_VERSION=3
          export OS_PROJECT_NAME=NeCTAR-Images
          IMAGES=$(openstack image list --long --public | grep 28eadf5ad64b42a4929b2fb7df99275c | grep NeCTAR | awk '{print $2}')
          for IMAGE in $IMAGES; do
            echo "==> Triggering tempest-image-check job for $IMAGE..."
            curl -s -X POST https://jenkins.rc.nectar.org.au/job/tempest-image-check/build \
              --user $JENKINS_API_USER:$JENKINS_API_TOKEN \
              --data-urlencode json="{'parameter': [{'name':'IMAGE', 'value':'$IMAGE'}, {'name':'AVAILABILITY_ZONE', 'value':'$AVAILABILITY_ZONE'}]}"
          done
    wrappers:
      - credentials-binding:
        - username-password-separated:
           credential-id: 7a2e4b77-a292-47a1-b852-c0cfd9c1c383
           username: OS_USERNAME
           password: OS_PASSWORD
        - username-password-separated:
           credential-id: jenkins-api
           username: JENKINS_API_USER
           password: JENKINS_API_TOKEN


- job:
    name: tempest-image-check
    defaults: global
    node: tempest
    scm:
      - git:
         url: ssh://review.rc.nectar.org.au:29418/internal/nectar-testing.git
         credentials-id: cd8b8dd3-b897-4ecb-985d-180d5b6f8498
    parameters:
      - string:
          name: 'IMAGE'
          description: "Image ID to test."
      - choice:
          name: 'AVAILABILITY_ZONE'
          description: 'Availability zone. Use "Any zone" to allow the scheduler to choose.'
          choices:
            - Any zone
            - auckland
            - intersect-01
            - melbourne-qh2
            - melbourne-qh2-uom
            - monash-01
            - monash-02
            - monash-03
            - qld
            - swinburne-01
            - tasmania
            - tasmania-02
            - tasmania-s
    builders:
      - shell: |
          #!/bin/bash -eu
          export OS_USERNAME=$OS_USERNAME
          export OS_PASSWORD=$OS_PASSWORD
          export OS_AUTH_URL=https://keystone.rc.nectar.org.au:5000/v3
          export OS_PROJECT_DOMAIN_NAME=Default
          export OS_USER_DOMAIN_NAME=Default
          export OS_IDENTITY_API_VERSION=3
          export OS_PROJECT_NAME=NeCTAR-Images
          openstack image show --max-width=120 $IMAGE
          default_user=$(openstack image show -f value -c properties $IMAGE | sed 's/, /\n/g' | grep default_user | sed "s/'//g" | awk '{print $NF}')
          . /opt/tempest/bin/activate
          tmpdir=`mktemp -d --suffix=_tempest`
          cd $WORKSPACE/tempest
          [ "$AVAILABILITY_ZONE" != "Any zone" ] && SET_AZ="-s $AVAILABILITY_ZONE"
          ./setup_tempest.py $SET_AZ -e production --image $IMAGE --username $default_user -j check-image $tmpdir
          cd $tmpdir
          stestr run --serial --whitelist-file $WORKSPACE/tempest/whitelists/check-image.yaml 2>&1 | grep --line-buffered -vE '   \w+Warning: |self._sock = None'
          RET=${PIPESTATUS[0]}
          rm -rf $tmpdir
          exit $RET
    wrappers:
      - credentials-binding:
        - username-password-separated:
           credential-id: 7a2e4b77-a292-47a1-b852-c0cfd9c1c383
           username: OS_USERNAME
           password: OS_PASSWORD
