# Runtime dependencies for tools/audit_projects.py
#
# jenkins-job-builder is also required (to render the JJB job set) but is the
# repo's primary tool; it is pinned in the top-level requirements.txt
# (jenkins-job-builder==6.5.0, matching jenkins-slave1).
ruamel.yaml>=0.17
requests>=2.25
